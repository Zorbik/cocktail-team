// import { createMarkupCocktail } from './createMarkupCocktail';
// import { gallery } from './renderStartMarkup';

// export function pagGallery(array, cardsPerPage, page) {
//   const newArray = array.splice(cardsPerPage * page - 1, page);
//   gallery.innerHTML = createMarkupCocktail(newArray);
// }

// export function numPagesGallery(number) {
//   const markup = '<div class="numPage__container"><div class="decrPage"></div>';
//   for (let i = 1; i <= number; i += 1) {
//     markup += `<div class="numPage">${i}</div>`;
//   }
//   markup += '<div class="incrPage"></div></div>';
// }

//
// ___________________________________________________
//
